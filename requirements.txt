# Virtual Engineer - Python dependencies
# Install with:  python -m pip install -r requirements.txt
fastapi==0.136.3
uvicorn[standard]==0.49.0
jinja2==3.1.6
pydantic==2.13.4
python-multipart==0.0.32

# Document ingestion (used by app/ingest.py for the analysis feature)
pypdf==6.13.1
python-docx==1.2.0
openpyxl==3.1.5
python-pptx==1.0.2
pillow==12.2.0

# Real Claude API + .env loading. The app auto-detects ANTHROPIC_API_KEY and
# uses Claude when present, otherwise falls back to the offline mock.
anthropic==0.107.1
python-dotenv==1.2.2
