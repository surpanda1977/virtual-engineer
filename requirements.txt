# Virtual Engineer - Python dependencies
# Install with:  python -m pip install -r requirements.txt
fastapi==0.136.3
uvicorn[standard]==0.49.0
jinja2==3.1.6
pydantic==2.13.4

# Optional - only needed if you switch from the mock to the real Claude API.
# Uncomment, then re-run the pip install command above.
# anthropic>=0.40.0
